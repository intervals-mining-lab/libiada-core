using System;
using System.Xml;
using System.Data;
using System.IO;
using System.Collections.Generic;
using System.Text;
using MDA.OIP.ScoreModel;

namespace MDA.OIP
{
    public class MusicXmlParser
    {
        /*
        XmlNodeList list = xd.GetElementsByTagName("measure"); // Создаем и заполняем лист по тегу "measure"  
        */			
			/*TODO вытащить все остальное из тактов*/
		/*	for (int i=0; i<list.Count; i++)
			{
				XmlNodeList newlist;
				XmlNodeList nnewlist;
				
				newlist = list[i].ChildNodes;
				for (int j=0; j<newlist.Count; j++)
				nnewlist= newlist[j].ChildNodes;
			}
        */
    public MusicXmlParser()
    {
        
    }
    }
}
